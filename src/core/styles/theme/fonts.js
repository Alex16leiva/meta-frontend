import colors from "./colors";

const h1 = {
    fontFamily: "Ubuntu,sans-serif",
    fontSize: "4vw",
    fontColor: "black",
};

const p = {
    fontFamily: "Arial",
    fontSize: "3vw",
    fontColor: "#0F1111",
    align: "justify",
    lineHeight: "16px",
};

const h2 = {
    fontColor: colors.palette.blue,
    fontSize: "16px",
    fontFamily: "verdana,arial,helvetica,sans-serif",
};

const fonts = {
    h1,
    p,
    h2,
};

export default fonts;
